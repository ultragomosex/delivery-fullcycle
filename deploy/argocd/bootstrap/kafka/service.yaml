apiVersion: v1
kind: Service
metadata:
  name: kafka
  namespace: kafka
  labels:
    app.kubernetes.io/name: kafka
    app.kubernetes.io/part-of: delivery-fullcycle
spec:
  ports:
    - name: plaintext
      port: 9092
      targetPort: 9092
  selector:
    app.kubernetes.io/name: kafka
