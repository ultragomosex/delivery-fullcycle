apiVersion: v1
kind: Service
metadata:
  name: forgejo
  namespace: forgejo
  labels:
    app.kubernetes.io/name: forgejo
spec:
  type: NodePort
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      nodePort: 30000
    - name: ssh
      port: 2222
      targetPort: 2222
      nodePort: 30022
  selector:
    app.kubernetes.io/name: forgejo
