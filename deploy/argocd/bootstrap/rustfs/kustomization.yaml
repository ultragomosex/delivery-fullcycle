apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: rustfs

resources:
  - namespace.yaml
  - secret.yaml
  - registry-secret.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml