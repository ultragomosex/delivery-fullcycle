apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: delivery-prod

resources:
  - ../../../base/frontend

namePrefix: prod-

patches:
  - target:
      kind: Deployment
      name: frontend
    patch: |-
      - op: replace
        path: /spec/replicas
        value: 3

images:
  - name: delivery-frontend
    newName: delivery-frontend
    newTag: v1.0.0
